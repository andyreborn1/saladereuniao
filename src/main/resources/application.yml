h2:
  console:
    path: /h2

jpa:
  database-platform: "org.hibernate.dialect.H2Dialect"
  hibernate:
    ddl-auto: update
  show-sql: true

spring:
  datasource:
    url: "jdbc:h2:mem"
    driverClassName: "org.h2.Driver"
    username: "sa"
    password: "sa"